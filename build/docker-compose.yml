version: "2"
services:
  web:
    build: ../
    ports:
     - "${APP_PORT}:3000"
    links:
      - redis
      - mongodb
    restart: always
    environment:
      - MONGO_URL=mongodb://mongodb:${MONGO_PORT}/${MONGO_DB}
    env_file:
      - configuration
      - ../environment_variables.env
  mongodb:
    image: "mongo"
    ports:
    - "${MONGO_PORT}:27017"
